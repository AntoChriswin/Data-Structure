#include<stdio.h>
int main()
{
	int i;
	int a[] = {1,2,3,4,5,6,7,8};
	int t = sizeof(a)/sizeof(a[0]);
	int key = 6;
	for(i=0;i<t;i++)
	{
		if(key == a[i])
		{
			printf("index %d is the value of key %d",i,key);
			return 0;
		}
	}
}
